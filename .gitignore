# Byggartefakter (rot och per-target).
/target
/target-win
/target-linux
/target-msvc
/target-gui
/test-build

# Färdiga releasepaket — OBS: mönstret måste vara rotankrat, annars
# matchar det även gui/dist (GUI:ts frontend, som SKA ligga i repot).
/dist

node_modules/

# Genererade Tauri-artefakter och inbakad sidecar-binär.
gui/src-tauri/gen/
gui/src-tauri/binaries/*.exe

.env
.env.*
!.env.example
